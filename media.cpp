#include <iostream>
#include <string.h>
#include <media.h>

using namespace std;

Media::Media(char* newTitle, int newYear){
  title = newTitle;
  year = newYear;
}
int* Room::getYear(){
  return year;
}
char[]* Room::getTitle(){
  return Title*;
}
